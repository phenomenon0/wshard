{
  "name": "wshard-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/viewer.ts --bundle --format=esm --outfile=dist/viewer.js --target=es2020",
    "serve": "npx http-server -p 5173 -c-1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
